# Harness coverage record: features -> scenarios

Which of spec #28's features have a scripted AFK-harness scenario proving them. Updated by each
sub-issue as it lands (#29-#46). See `docs/dev-setup.md` for how to run the harness and
`decomp-so/reference/coverage.md` for the *reconstruction's* function-level coverage (a different,
earlier record from spec #16 - that one tracks decompiled functions against reference files; this
one tracks spec #28 features against harness scenarios).

- **Status:** `covered` = a scripted scenario in `tools/` (or a documented harness invocation)
  exercises the feature and the harness's checks pass (or, for #29 only, are expected to report
  red - see its row). `pending` = no scenario yet.
- **Scenario** names the harness entry point that exercises the feature.

## Foundation

| Feature | Scenario | Status |
|---|---|---|
| `chextrek.dll` builds (32-bit x86) from stock dhewm3-sdk, pinned at `ad837f9b1b` | `tools/build-chextrek.sh` | covered |
| AFK harness launches dhewm3 1.5.5 win32 with the mod mounted, drives it via a console script ending in `quit`, kills a hung run after a timeout | `tools/run-harness.sh` | covered |
| Developer-only state-dump console command (`chextrek_dump`), stable header line | `tools/run-harness.sh` asserts `CHEXTREK-STATE-DUMP v1` in the log | covered (header only - #29 lands the skeleton; later sub-issues add state under it as their features land) |
| Harness proves *chextrek.dll* (not `base.dll`) loaded | `tools/run-harness.sh` asserts `loaded game library '...chextrek.dll'` and the state-dump header | covered |
| Harness run to the main menu is green (script compile passes, menu loads) | `tools/run-harness.sh`, `tools/test-menu-smoke.sh` | covered - flipped green by #30 (was expected red under #29; `tools/test-menu-smoke.sh` replaces the old `tools/test-tracer-bullet.sh`, which asserted the opposite) |
| Always-on checks: no `ERROR`, no unknown event/spawnclass, no script-compile error, map finishes loading | `tools/run-harness.sh` (main menu only); `tools/test-script-events.sh` (loads `e1m1`, asserts `<N> msec to load e1m1`); `tools/test-objectives.sh`, `tools/test-item-text.sh` (load `sf_923`, assert `<N> msec to load sf_923`); `tools/test-end-level-stats.sh` (loads both `e1m1` and `sf_923` in separate runs, asserting both load messages) | covered. The unknown-spawnclass check (`tools/lib-harness.sh`) matches the engine's real message (`Could not spawn '<classname>'.  Class '<spawnclass>' not found` - the "Unknown spawnclass" text #29 guessed never actually appears). #30-#32 allowlisted one pre-existing, tracked gap by class name: `idTarget_EndLevelGUI` wasn't implemented yet, so `target_endlevelgui_1`/`_2` on both `e1m1` and `sf_923` failed to spawn independently of those sub-issues' own changes. #33 ports `idTarget_EndLevelGUI` (below, "Custom UI + end-level stats") and removes that allowlist entry: both maps now spawn it cleanly, checked by every scenario in this row that loads either map. |

## Per-feature (spec #28 order; #30 onward)

| Feature | Scenario | Status |
|---|---|---|
| Script events `openDoors` (`idAI::OpenDoors`/`Event_OpenDoors`), `setProj`, `spawnDict`, `footPrint` (anim frame command + `PlayFootStepSound` lead); `idActor` extra `EV_Remove` entry; `ProjectDecal` 8-arg overload | `tools/test-script-events.sh` | covered - each event's observable effect: `openDoors` opens `e1m1`'s `func_door_17`, `setProj` changes the weapon's `createProjectile()` classname, `spawnDict` spawns an entity from a def, `footPrint`/`ProjectDecal` show up in `chextrek_dump`'s `footprints:` counter, and a script's `remove()` shrinks `chextrek_dump`'s `entities:` counter. `idActor`'s extra `EV_Remove` entry is ported for fidelity to the reconstruction but is behaviorally a no-op (routes to the same `idClass::Event_Remove` already inherited) - not separately observable, see `decomp-so/reference/script-events.md` Notes. |
| Objectives (`mkObjective`) + `idPlayer::addObjective`/`freeObjective`/`addItemText` | `tools/test-objectives.sh`, `tools/test-item-text.sh` | covered - loads `sf_923` (the only map with `trigger_objective` entities) and triggers both `trigger_objective_1` and `trigger_objective_2`: triggering fills `chextrek_dump`'s `objective_slot_N` with the objective's title, a second trigger on `trigger_objective_1` empties its slot without disturbing `trigger_objective_2`'s, and a `savegame`/`loadgame` round-trip leaves `trigger_objective_2` (the only one still active going into the save) attached and shown in the dump. `idPlayer::HudMapLevel`, which `addObjective` calls to fill `mkObjective::mapLevel`, was a stub returning 0 (`Player.cpp`) pending `idPlayer::mapLevels[]` and the rest of the HUD map group's members - #36 (below) replaces it with its real body (`decomp-so/reference/hud-map.md`); nothing reads `mkObjective::mapLevel` yet (that's `updateMapUI`'s objective-icon coloring, also ported by #36 but inert until an objective and the HUD map are both up on the same map at once - `sf_923`, the only map with objectives, sets no `map_level_N`, so every objective there is level 0 already). **Confirmed live, not just inferred from the reference:** after a save/load, a lone active objective can come back in a *different* slot than it had before saving (`trigger_objective_2` came back as `objective_slot_1`, not `_2`) - `decomp-so/reference/objectives.md`'s Notes call this out as an open question (`nextObjective` isn't saved, and it's the reconstructed `idPlayer`'s *constructor* that zeroes it back to 0 on the reload - `idPlayer::Restore` never calls `Init()` - so re-attachment order, not original slot, decides the new slot). This is the reconstructed mod's actual behavior, not a harness bug or a regression to fix (spec #28 targets original behavior, not enhancements), so the scenario asserts the exact, deterministic post-load slot (`objective_slot_1`) this produces here, rather than treating the renumbering as untestable. A second `savegame`/`loadgame` immediately after (from that already-in-slot-1 state, where the objective is already sitting in the first free slot) checks the literal wording of the acceptance criterion too: that round-trip leaves `objective_slot_1`/`_2` unchanged, the "same objective slots" case. **`addItemText` (spec #32):** now separately asserted by `tools/test-item-text.sh`, not just exercised as a side effect. `addItemText`'s only callers in the reconstructed binary are `mkObjective::AttachToLocalPlayer`/`RemoveFromLocalPlayer` (`decomp-so/reference/objectives.md`'s Notes), so "picking up an item [that] shows its item text" means picking up an item that targets a `trigger_objective`. `maps/sf_923.map`'s own `moveable_item_pistol_1` (`target1` `trigger_objective_1`) is that item - but `trigger_objective_1` is itself first triggered by `trigger_once_6` (`target3`, along with `trigger_objective_2` via `target4`) earlier in a real playthrough, which *attaches* both objectives (`addmsg` "New Objective" each); the pistol pickup is `trigger_objective_1`'s *second* trigger, which *removes* it (`rmmsg` "Objective Complete") rather than showing "New Objective" again. The scenario reproduces both triggers in that order so the item text it asserts after the pickup ("Objective Complete") matches what an actual playthrough would show, not an order the map never produces. Because `idPlayer::UpdateHud` drains `inventory.pickupItemNames` into HUD GUI state within a frame or two of `addItemText` filling it, the queue itself isn't reliably observable from a scenario's `chextrek_dump` (which runs after a `wait`); a test-only hook (`ChexTrek_NoteItemTextShown`, called from `addItemText` itself, `Player.cpp`, the same "hook added to already-ported game code" pattern as spec #30's `ChexTrek_NoteFootprintProjected`) records a call count and the last name *queued*, printed as `chextrek_dump`'s `item_text_count`/`item_text_last` lines. This proves `addItemText` ran with the expected name (spec #32's AC), not that `UpdateHud` went on to render it on screen - `decomp-so/reference/objectives.md`'s Notes flag that render path (`hud.gui`'s commented-out `invPickup`/`itemicon` wiring) as not checked. |
| Custom UI (`idCustomUI`) + end-level stats (`idTarget_EndLevelGUI`), incl. stat counting | `tools/test-end-level-stats.sh` | covered - `idCustomUI` (`decomp-so/reference/custom-ui.md`) and `idTarget_EndLevelGUI` (`decomp-so/reference/end-level-stats.md`) ported in full, including `idPlayer::useCustomUI`/`clearCustomUI`/`incSecretsFound`/`getLevelStats`/`levelStats[4]`, `idGameLocal::GetLevelStats` and `idStr::FormatTime`. **Out of scope, not needed by #33's AC:** the `g_PDA` cvar and `idCmdSystem::ArgCompletion_GuiName` that `custom-ui.md` bundles into the same Ghidra export group - their only registration site is the PDA GUI file (spec #28 story 19, a separate feature the reference's own Notes say doesn't belong to this group functionally), not anything `idTarget_EndLevelGUI` reads. Scenario: on `e1m1`, spawns a monster and radius-damages it with the player as attacker (`sys.radiusDamage`, since the `kill` script event credits the AI itself, not the player - `idAI::Event_Kill` - and wouldn't exercise the edit-inside-`AddAIKill` lead), spawns and picks up a fresh `ammo_bullets_small` (not one of `e1m1`'s own placed items - a health-only item's `idPlayer::GiveItem` silently no-ops when the player is already at full health, which the reference's `gave` check means never counts as a pickup either), and triggers a `secret`-flagged `func_door_16`: `chextrek_dump`'s new `level_stats: monsters=<found>/<total> items=<found>/<total> secrets=<found>/<total>` line rises by exactly one across each action. Then triggers `target_endlevelgui_1` (`g_statTicTime` set to 1 first so the counting animation catches up in well under a second instead of the default ~50ms/tic pacing) and confirms the dump's new `customui: none -> active` line plus `customui_gui_ai_killed`/`items_found`/`secrets_found` (read from the GUI's own state, `idUserInterface::State()`) match `level_stats`. A second, shorter run loads `sf_923` and triggers its own `target_endlevelgui_1` the same way, covering the AC's "both maps" requirement for the half `e1m1`'s run doesn't reach. **Edits-inside-stock-functions leads recorded for #33** (not in the #16 export, since `idMover_Binary::Use_BinaryMover` and `idPlayer::GiveItem(idItem*)` are stock, pre-existing functions, same pattern as #30-#32's leads). Both are **checked against the `gamex86.so` disassembly** using `decomp-so/verify`'s own tooling (`capstone`/`pyelftools`, already listed in its `requirements.txt`): `idPlayer::GiveItem(idItem*)` increments `levelStats[1].found` only when it both actually gave something (`gave == true`) *and* the picked-up item's own `spawnArgs` has `"level_item" "1"` (`idPlayer::GiveItem` @ 0x164cd0; the guarding `FindKey`/`atoi` sequence at 0x164f1d-0x164f64 resolves to the rodata string `"level_item"` - the same key `idGameLocal::GetLevelStats` counts into `levelStats[1].total`); the secret counter is credited by two of `idMover_Binary`'s own new members, `secret` (mirrors the `"secret"` spawnArg, set once in `Spawn`, disassembly 0x1384e5) and `secretFound` (guards against double-crediting; both zeroed in the constructor, 0x132f78/0x132f7f), checked at the very top of `Use_BinaryMover` (0x136c70) - before even the "only the master should be used" redirect, so it runs at every level of a moveMaster chain via that redirect's recursion - and skipped entirely in multiplayer (`gameLocal.isMultiplayer`). `idPlayer::AddAIKill` increments `levelStats[0].found` unconditionally, before its existing soul-cube-ammo early return; this one matches the reference's own cited address (0x15e878) and wasn't separately re-checked against the disassembly here. **Open lead, not confirmed:** whether the binary's own `idMover_Binary::Save`/`Restore` persist `secret`/`secretFound` - this port saves/restores both anyway (so a save/load round-trip can't re-credit an already-found secret), a deliberate, documented departure if the binary doesn't. |
| Stats-screen `nextmap`/`skip` commands (`idTarget_EndLevelGUI::HandleCustomGUICommand`, spec #28 story 18) | `tools/test-end-level-nextmap.sh` | covered - the method itself was already ported in full by #33 (`Target.cpp`); #34 is scenario coverage proving its two GUI commands actually work as `decomp-so/reference/end-level-stats.md` describes them. Both commands only ever reach the game, in the real game, through a mouse click on `guis/chex/stats.gui`'s `skip`/`nextmap` buttons (`onAction` -> `idPlayer::Weapon_GUI`'s `BUTTON_ATTACK` handling -> engine-side `idUserInterface::HandleEvent`), which needs real mouse-delta input (`usercmd_t::mx`/`my`) the console-only harness (`map`, `script`, `setviewpos`, `trigger`, `spawn`, `impulse`, `wait`) can't produce. **New test-only console command, `chextrek_customui_cmd <command>`** (`ChexTrekDump.cpp`/`.h`, registered in `gamesys/SysCmds.cpp`) closes that gap the same way the state-dump command closes its own: it reads one token the same way `idPlayer::HandleSingleGuiCommand` would and passes it to the local player's registered `idCustomUI` through the exact same virtual call a real click reaches (`HandleCustomGUICommand`) - everything downstream is the already-ported #33 code, unchanged; the command itself changes no game behavior beyond what a click would have produced. Scenario: on `e1m1`, reuses #33's kill/pickup/secret setup so `level_stats` isn't trivially 0/0, triggers `target_endlevelgui_1`, waits just long enough (10 frames, comfortably above the one default-paced `g_statTicTime` tic needed to flip the screen's internal state from "not started" to counting the first line, comfortably below the ~4 tics needed to finish that first line on its own - see the script's own header comment for why a `g_statTicTime` override was tried and abandoned) for the screen to have started but not finished counting, then sends `chextrek_customui_cmd skip` four times back to back (no `wait` between, so no naturally-scheduled tic can interleave): each jumps the current line (monsters, items, secrets, then the level-time line) straight to its final value, checked against `chextrek_dump`'s `level_stats`/`customui_gui_*` lines rather than a hardcoded number. A guard assertion (dump #3's `ai_percent`, before any `skip`, strictly below the final target) catches the one failure mode a bad wait could cause: natural counting finishing the monsters line before the scenario's own `skip` calls start, which would silently shift every later `skip` onto the wrong line while still reporting "handled". On `sf_923`, `idTarget_EndLevelGUI`'s "nextmap" GUI command is *not* how the shipped map actually reaches `e1m1`: neither of its own `target_endlevelgui` entities has a `nextmap` spawnArg (Notes: "set on no `target_endlevelgui` in `maps/`"), and the map's real `"nextMap" "e1m1"` key (`maps/sf_923.map:35106`) lives on `target_endlevel_3`, a stock `idTarget_EndLevel` reached only via a separate script (`script/map_storage_facility.script`) off `target_endlevelgui_2`'s `end_trek.gui` - none of that chain is `idTarget_EndLevelGUI::HandleCustomGUICommand` or anything #33/#34 ported, and driving it is out of scope here. Since no shipped map ever gives `idTarget_EndLevelGUI`'s own `nextmap` spawnArg a value, the scenario spawns a fresh entity that does (`nextmap` `e1m1`; `spawn` supports extra key/value pairs, same as any other spawnclass) - the only way to exercise that specific, ported mechanism at all, and a direct reading of AC2's literal wording ("nextmap ... loads e1m1"). Triggers it, sends `chextrek_customui_cmd nextmap` (no state guard on this command, so it works even before the first tic), confirms the log actually shows `chextrek_customui_cmd: 'nextmap' handled` (so a later, coincidental map load can't pass this on its own), and confirms `e1m1` finishes loading next - the same `"<N> msec to load <map>"` evidence every other map-load assertion in this table already uses. **Recorded deviation from spec #28's Implementation Decisions** (which describe the state-dump command as "the only test code in the library"): `chextrek_customui_cmd` is a second test-only console command, needed because nothing in spec #28's console-only command list can simulate the mouse click these two GUI commands otherwise require - the same class of gap the state-dump command exists to close, on the driving side rather than the observing side. Gated `CMD_FL_CHEAT` plus its own `CheatsOk( false )` check, matching `Cmd_Trigger_f`/`Cmd_Spawn_f` - honestly weaker than "developer-only" (spec #28 story 34, which the state-dump command matches): `CMD_FL_CHEAT`/`CheatsOk()` only block non-cheat multiplayer clients, not single-player without `developer 1` (an explicit `developer.GetBool()` check was tried and reverted after it broke the harness scenario for an untracked reason). Narrower in practice, though not in principle (multiplayer with `net_allowCheats 1` still passes the gate): the command can only change anything when a player-triggered `idCustomUI` is already registered. |
| `g_PDA` cvar + `idCmdSystem::ArgCompletion_GuiName` (spec #28 story 19) | `tools/test-pda.sh` | covered - `decomp-so/reference/custom-ui.md`'s previously-deferred half (see #33's row above, "out of scope"). `g_PDA` (`Player.cpp`) is ported verbatim (default `"guis/pda_chex.gui"`, `CVAR_GAME \| CVAR_ARCHIVE`, completion set to `idCmdSystem::ArgCompletion_GuiName`); `ArgCompletion_GuiName` itself (`framework/CmdSystem.h`) is a one-line `ID_INLINE` forwarding to `cmdSystem->ArgCompletion_FolderExtension( args, callback, "guis/", false, ".gui", NULL )`, matching the reference exactly, placed next to stock's `ArgCompletion_MapName` as the reference's own Notes describe. **Edit-inside-stock-function lead recorded for #35** (not in the #16 export - `idPlayer::Spawn` is a stock, pre-existing function, same pattern as #30-#34's leads): `idPlayer::Spawn` (`Player.cpp`) loads `objectiveSystem` from `g_PDA.GetString()` in place of stock's hardcoded `"guis/pda.gui"` literal - the reference's own Notes cite this call site (`idPlayer::Spawn @ 0x16f730`) as `g_PDA`'s only read. Scenario, in two `map e1m1` phases (a fresh `map` re-runs `idPlayer::Spawn`, so this is the only way to see two different `g_PDA` values take effect in one run): **phase 1** explicitly `set`s `g_PDA` to a *third*, unrelated existing gui (`guis/chex_credits.gui` - neither stock's own hardcoded literal `guis/pda.gui` nor the mod's `g_PDA` default `guis/pda_chex.gui`, so a pass can't be explained by either of those coincidentally matching) before `map`, and `chextrek_dump`'s new `pda_gui` line (`idPlayer::objectiveSystem->Name()`, `ChexTrekDump.cpp`) shows that exact value immediately after spawn, before the PDA is ever opened - proving `idPlayer::Spawn` really reads whatever `g_PDA` currently holds, not a hardcoded literal of any kind; **phase 2** resets `g_PDA` to its own default (`guis/pda_chex.gui`) and `map`s `e1m1` again, satisfying the AC's literal wording ("with `g_PDA` set, open the PDA; dump/log shows the mod's PDA GUI is the active one") with the mod's real, shipped configuration - not just an arbitrary override. In both phases, since `idPlayer::TogglePDA` refuses to open with no PDA owned (`inventory.pdas.Num() == 0` shows a "no PDA" tip instead) and neither shipped map places a reachable PDA pickup, the scenario spawns `def/items.def`'s `item_pda` (stock `idPDAItem`, not anything #35 ports; given an explicit `name` so `trigger` doesn't depend on `Cmd_Spawn_f`'s auto-generated numbering) and `trigger`s it: `idItem::Event_Trigger` with the player as activator (`Cmd_Trigger_f` passes the local player) picks it up synchronously, and `idPlayer::GivePDA` (stock, unedited) calls `TogglePDA()` itself on the player's first PDA - opening it without needing impulse/mouse input. `pda_open` flips `0 -> 1` in both phases while `pda_gui` stays whatever that phase's `g_PDA` named - phase 2 ending on `pda_gui: guis/pda_chex.gui` / `pda_open: 1` is the AC's exact claim. **New test-only console command, `chextrek_test_gui_completion`** (`ChexTrekDump.cpp`/`.h`; read-only, `CMD_FL_GAME` only like `chextrek_dump`, not gated like `chextrek_customui_cmd`'s state-changing `CMD_FL_CHEAT`; a **recorded deviation from spec #28's "the [state-dump command] is the only test code in the library"**, the same class of gap `chextrek_customui_cmd` (#34) and `chextrek_dump` itself already are - here on the "prove a function pointer, not just its existence" side): tab-completion itself isn't reachable from a console script, and calling `ArgCompletion_GuiName` directly would only prove the function exists, not that `g_PDA` is wired to it - so this command looks `g_PDA` up via `cvarSystem->Find`, reads its own `idCVar::GetValueCompletion()` (the exact pointer real tab-completion would call), prints whether the cvar was found (`g_pda_found`) and whether that pointer equals `idCmdSystem::ArgCompletion_GuiName` by address (`g_pda_completion_wired`), then calls it and dumps every result (`gui_completion_count`, `gui_completion_<N>`). The scenario asserts all three: the cvar exists, its completion is really that function (not just some non-NULL one), and one result names `guis/pda_chex.gui` (the mod's own default). |
| HUD map | `tools/test-hud-map.sh` | covered - `decomp-so/reference/hud-map.md`'s `idPlayer::initHudMap`/`updateMap`/`updateMapUI`/`updateHudMapAlpha`/`MapImageCoords` ported in full, plus `HudMapLevel`'s real body (spec #16/#31 left it a stub returning 0 pending this sub-issue - see the "Objectives" row above). **Edits-inside-stock-functions leads recorded for #36** (all in `Player.cpp`, matching the reference's own leads list): `idPlayer::Spawn` calls `initHudMap()` right after loading `objectiveSystem`; `idPlayer::Think` calls `updateMap()` every frame (just before `UpdateHud()`); both `idPlayer::Init` and `idPlayer::PerformImpulse`'s impulse 23 case are edited as below. **Scope, matching spec #28's per-sub-issue split of story 13:** this sub-issue is only the map itself, toggled by impulse 23, with fog of war revealed as the player moves - the PDA's `map_scroll_*`/`map_zoom_*`/`map_scroll_center` GUI commands that drive `idPlayer::mapControl` (`idPlayer::HandleSingleGuiCommand`, not edited here - `mapControl` stays 0) are #37, the `showMap` console command is #38, and saving/restoring this state across a save/load is #39. Scenario: on `e1m1`, `chextrek_dump`'s new `hud_map: level=<N> visible=<0|1> coverage=<N>` line (`ChexTrekDump.cpp`) - `visible` reads the HUD gui's own `"HudMap"` state flag directly (`idPlayer::hud->GetStateBool`), `coverage` counts alpha-revealed texels in the current level's 128x128 fog-of-war image (`hudmap_alpha`, the extern global the reference itself declares, defined in `Player.cpp` and zeroed by `idPlayer::Init`, spec #28's "the fog of war is a single global, not per player" behavior called out in the reference's own Notes). **AC1** (impulse 23 toggles the map): the console-only harness has no plain `impulse` console command, and typing the actual bind-target token (`_impulse23`, what `autoexec.cfg`/`matt.cfg`/`scott.cfg` bind keys to per the reference's Notes) is rejected too - both confirmed `Unknown command` against this engine build, since `idUsercmdGenLocal` only recognizes `"_impulseN"` strings from a real, currently-held bound key. **New test-only console command, `chextrek_test_impulse <N>`** (`ChexTrekDump.cpp`/`.h`, `CMD_FL_CHEAT` plus its own `CheatsOk( false )` check, same gate as `chextrek_customui_cmd` from #34) closes that gap the same way: it calls `idPlayer::PerformImpulse(N)` directly, so everything downstream (the switch in `PerformImpulse`, `HandleNamedEvent`, `hud.gui`'s own `hudmap_open`/`hudmap_close` `onNamedEvent` blocks) is the real, already-ported game code - a **recorded deviation from spec #28's "the state-dump command is the only test code in the library"**, the same class of gap `chextrek_customui_cmd` (#34) and `chextrek_test_gui_completion` (#35) already are. The scenario sends `chextrek_test_impulse 23` twice (open, then close) and asserts `visible` goes `0 -> 1 -> 0`. **Timing fix (found while landing #38, same class of gap #37 hit and fixed in `tools/test-pda-map.sh`):** `hud.gui`'s `hudmap_close` window doesn't flip `gui::HudMap` back to 0 until its own `onTime 400` (400ms into its fade-out transition, versus `hudmap_open`'s `onTime 5`), timed off `gameLocal.time` (`idPlayerView::SingleView`'s per-frame `Redraw( gameLocal.time )`), not off a fixed count of console-script `wait` ticks - and the two don't map 1:1, so a single fixed-length wait (originally `wait 40`) followed by one `chextrek_dump` landed on either side of the flip across runs, deterministically reproducing at both the branch tip and #36's own commit. Rather than guess a bigger fixed margin (liable to the same intermittent failure, just at lower odds), the scenario now samples repeatedly after the second `chextrek_test_impulse 23`: eight `chextrek_dump`s, 20 frames apart (160 frames of total margin), and the assertion passes if *any* of the eight already shows `visible=0` - the flip is one-time and monotonic (`idWindow`'s own `onTime` semantics: a timeline fires once and only `resetTime` rearms it, and nothing here calls `resetTime "hudmap_close"` again), so once one sample sees 0, every later sample would too; the last of the eight doubles as AC2's pre-`setviewpos` coverage baseline. Verified green across several repeated back-to-back runs after the fix. **AC2** (setviewpos to several points; coverage grows after each move): `noclip` (stock) is set first so `setviewpos`'s teleport can't be blocked or corrected by collision against `e1m1`'s geometry - irrelevant to fog-of-war, which only reads the player's origin. A baseline dump (standing at the spawn point) plus 3 `setviewpos` calls each jump 1300+ world units in at least one axis from the previous point (`e1m1`'s own `map_coords` make one texel ~27.5 world units, and `e1m1`'s own worldspawn `map_radius` of 12 texels - the code's own default, `Player.cpp`, is 8 - reveals a square roughly 660 world units on a side around each point, so a smaller jump risks landing entirely inside ground already fully revealed, showing no growth even though the feature works - observed while writing this scenario with closer points, including a first move that landed back on the already-revealed spawn point itself) and asserts `coverage` strictly increases each time, including the first move from the baseline. |
| PDA map `map_*` zoom/scroll/center commands | `tools/test-pda-map.sh` | covered - the edit-inside-stock-function lead `decomp-so/reference/hud-map.md`'s Notes recorded for #36 but scoped out of it: `idPlayer::HandleSingleGuiCommand` (`Player.cpp`) now recognizes `map_zoom_in`/`map_zoom_out` (OR in their `mapControl` bit), `map_scroll_center` (OR in `MAP_CENTER`), the four `map_scroll_*` commands (replace `mapControl` with their own bit, matching `updateMapUI`'s `==`-based scroll switch, which only scrolls while no other bit is set), and `map_stop` (keeps `MAP_CENTER` only if a zoom bit was set alongside it, else clears `mapControl` to 0) - exactly as the reference's Notes describe (`idPlayer::HandleSingleGuiCommand`, 0x16d96e-0x16db5b). #36 already ported the consumer of these bits (`updateMapUI`, called every frame from `updateMap`/`Think`), so this sub-issue is purely the producer side. **New test-only console command, `chextrek_test_map_cmd <command>`** (`ChexTrekDump.cpp`/`.h`, `CMD_FL_CHEAT` plus its own `CheatsOk( false )` check, same gate as `chextrek_customui_cmd`/`chextrek_test_impulse`): these GUI commands only ever arrive, in the real game, from a mouse click on the PDA map's arrow/zoom/center buttons (`guis/pda.gui`, `guis/pda_chex.gui`) - the same class of gap `chextrek_customui_cmd` (#34) closed for the end-level stats screen's buttons - so this command calls the local player's own `idEntity::HandleGuiCommands( player, cmd )`, the exact stock entry point a real GUI `onAction` reaches, meaning everything downstream is the real, already-ported game code, unchanged. A **second new test-only command, `chextrek_test_pda_map_open <0|1>`**, was needed for a separate reason found while writing the scenario: `idPlayer::updateMap` only ever applies `mapControl`'s bits to the PDA's own map page (as opposed to the always-centered HUD corner map) while the PDA is open *and* the PDA gui's own `"HudMap"` state variable is true - a variable only a mouse click on `guis/pda_chex.gui`'s "Data" tab sets in the real game (`resetTime "hudmap_open" "0"`, which sets `"gui::HudMap" "1"` at its own `onTime 5`), not anything this sub-issue's C++ drives, and out of the console-only harness's reach the same way the `map_*` commands themselves are; this command sets that same GUI state variable directly through `idUserInterface::SetStateBool` - the same underlying engine call the GUI script's own `set "gui::HudMap" "1"` resolves to (not literally the same call site: the real click goes through the window-script interpreter, this calls `SetStateBool` directly) - with no new C++ decision logic. **GUI-script timing quirk found while writing the scenario, not a C++ gap:** `guis/pda_chex.gui`'s `hudmap_close` window has no `notime 1` the way `hudmap_open` does, so its own `onTime 400` fires once, flipping `gui::HudMap` back to 0 on its own, regardless of whether the map page was ever entered; the only other callers that reset that window's timeline (`resetTime "hudmap_close" "0"`, `guis/pda_chex.gui`'s Data/Stats tab buttons) are themselves mouse-click handlers the harness never fires, so it never refires. That flip is timed off ~400ms of `gameLocal.time` (`idPlayerView::SingleView` calls `player->objectiveSystem->Redraw( gameLocal.time )` every frame the PDA is open, `PlayerView.cpp`) since the PDA gui was created, not off a fixed number of console-script "wait" ticks - and the two don't map 1:1, so a fixed frame-count margin can't reliably bound it. Reproducing the scenario repeatedly (probing `map_pda`'s `view_x`/`view_y` at fine grain, with no re-assert after the initial one) showed the flip lands at a different "wait"-tick count on almost every run, and once it lands, `mapView`/`mapScale` stay frozen for the rest of the run (movement never resumes on its own - consistent with `idWindow`'s own `onTime` semantics: a timeline fires once and only `resetTime` rearms it, and nothing here ever calls `resetTime "hudmap_close"` again). Two earlier fixes each assumed a fixed frame-count margin was enough to place a single re-assert safely past the flip (first, re-sending before every step; then, replaced by a single 60-frame wait up front) - both still lost a run's movement intermittently, for exactly that reason. This version instead re-sends `chextrek_test_pda_map_open 1` immediately before every measured step and after every 5-frame chunk of every wait (except the last chunk right before a `chextrek_dump`, since nothing after it still needs the flag true), so the flip, whenever it lands, is always corrected again within a handful of frames, well before the next measured dump - verified with four back-to-back full-scenario runs, all green. Both new commands are further **recorded deviations from spec #28's "the state-dump command is the only test code in the library"**, the same class of gap `chextrek_customui_cmd` (#34), `chextrek_test_gui_completion` (#35) and `chextrek_test_impulse` (#36) already are. `chextrek_dump`'s new `map_pda: scale=<f> view_x=<f> view_y=<f> control=<N>` line (`ChexTrekDump.cpp`) reads `idPlayer::mapScale`/`mapView`/`mapControl` directly. Scenario, on `e1m1` (PDA opened the same way as #35's `tools/test-pda.sh`: spawn+trigger a fresh `item_pda`, since `idPlayer::GivePDA` calls `TogglePDA()` itself on the player's first PDA), exercises all eight `map_*` commands and both branches of `map_stop`: baseline shows `control=0`; `map_zoom_in` grows `scale` and sets `control=2`; `map_stop` (its "clear to 0" branch, since no `MAP_CENTER` is set yet - proven by the following `map_zoom_out` landing on a clean `control=4`, not `4|2=6`) then `map_zoom_out` shrinks `scale` back down; `map_stop`+`map_scroll_right` grows `view_x` from 0 and sets `control=64`; `map_stop`+`map_scroll_up` grows `view_y` from 0 (leaving `view_x` unchanged) and sets `control=8`; `map_stop`+`map_scroll_down` shrinks `view_y` back down (leaving `view_x` unchanged) and sets `control=16`; `map_stop`+`map_scroll_left` shrinks `view_x` (leaving `view_y` unchanged) and sets `control=32`; `map_stop`+`map_scroll_center` moves `view_x`/`view_y` away from the scrolled position (to the player's spawn origin, per `updateMapUI`'s centering logic - not separately asserted against a raw origin value, since `chextrek_dump` has no such line to compare against) and sets `control=1`; a second `map_zoom_in` (with `MAP_CENTER` still set) grows `scale` again and sets `control=3` (`MAP_CENTER|MAP_ZOOM_IN`, proving zoom ORs in rather than replacing); `map_stop`'s *other* branch is then proven directly: with a zoom bit set alongside `MAP_CENTER`, it keeps `control=1` (not 0); a second, immediate `map_stop` (now with only `MAP_CENTER` and no zoom bit) falls into the "clear to 0" branch again. Every command's bit and its numeric effect (or, for `map_stop`, its two distinct outcomes) is checked directly against the dump, independent of GUI rendering. |
| `showMap` console command | `tools/test-show-map.sh` | covered - `decomp-so/reference/hud-map.md`'s `idPlayer::Cmd_ShowMap_f` ported in full, registered as the `"showMap"` console command by `idGameLocal::InitConsoleCommands` (`gamesys/SysCmds.cpp`) exactly where the reference's own disassembly excerpt places it: right after the last stock command (`"testid"`) and before the mod's `"takeEnvShots"` (env-shots group, not yet ported), with the reference's exact name/flags/description (`CMD_FL_GAME|CMD_FL_CHEAT`, `"show the whole map"`, no completion hook). Unlike #36/#37's `chextrek_test_*` commands, this is a real game command, not test-only harness plumbing - there is no wiring gap to close, since `showMap` is a self-contained console command the reference's Notes describe as reading its own registration strings straight from the disassembly (`idGameLocal::InitConsoleCommands` is stock and not itself edited). Scenario: on `e1m1`, a baseline `chextrek_dump` (partial, nonzero coverage, standing at the spawn point after the initial load wait, same as #36's own baseline) is followed by `showMap 1` (the single-level argument form, targeting level 1 - the player's own level is 0) and a second `chextrek_dump`, asserted at the *same* coverage as the baseline, proving the argument form doesn't spill onto the player's own level when given a different one (not a full proof that it fills only the level it names and nothing else - `chextrek_dump`'s `hud_map` line only ever reports the player's *current* level, so there's no way for this harness to directly confirm level 1's own coverage after `showMap 1`; a `showMap 1` that quietly did nothing at all would pass the same assertion). A third dump, after a plain `showMap` (no level argument, filling every level's `hudmap_alpha` image with `0xff`), is asserted at the full `coverage=16384` (128x128 fog-of-war texels) - the AC itself, and the one assertion that actually exercises `Cmd_ShowMap_f`'s effect. No wait-for-upload is needed: `Cmd_ShowMap_f` doesn't call `idRenderSystem::UploadImage` itself (per the reference's own Notes, the change "shows on screen at the next reveal"), but `chextrek_dump`'s `coverage` count (#36) reads `hudmap_alpha` directly, not the uploaded render texture, so the full reveal is visible in the very next dump with no wait. |
| HUD map state survives save/load | `tools/test-hud-map-saveload.sh` | covered - `decomp-so/reference/hud-map.md`'s Save/Restore lead ported into `idPlayer::Save`/`Restore` (`Player.cpp`): `mapScale`/`mapRadius`/`unknown1e5c`/`revealDistance`/`mapWidth`/`mapHeight`/`mapCoords`/`mapMaterial`, then the whole `hudmap_alpha` fog-of-war global, written and read back in that exact order (the reference's own disassembly writes the fog-of-war global one `WriteByte` per byte; this port uses a single bulk `savefile->Write`/`Read` instead - same bytes, same order, matching the SDK's own existing convention for byte-array members, e.g. `Target.cpp`'s `idTarget_EndLevelGUI::Save`/`Restore` of `displayStats`). Per that same lead, `mapControl`/`mapView`/`lastRevealOrigin`/`mapLevels` are **not** saved: the first three are transient PDA/reveal-tracking state, and `mapLevels` is normally re-read from the world's spawnArgs by `initHudMap()` every time `idPlayer::Spawn` runs (a fresh map load) - never by a save/load. **Gap found and closed while writing this sub-issue's scenario:** a savegame load (`idGameLocal::InitFromSaveGame`, `Game_local.cpp`) restores straight from the savegame's serialized objects and never calls `Init()`/`Spawn()` on the player object it restores into, so without also giving these four fields (`mapControl`/`mapView`/`lastRevealOrigin`/`mapLevels`) sane defaults in the `idPlayer` constructor itself (`Player.cpp` - a no-op on a normal spawn, where `Init()`/`initHudMap()` overwrite them again right after construction anyway), a freshly-allocated `idPlayer` restored from a savegame held whatever garbage was already in that memory (`Mem_Alloc` doesn't zero it) - confirmed live: a garbage `mapLevels[0]` landing above the player's own z fired `idPlayer::HudMapLevel`'s "Location below lowest MapLevel" warning after every save/load round-trip the scenario ran, before this fix. This answers `decomp-so/reference/hud-map.md`'s own open question ("whether `idPlayer::Spawn` (hence `initHudMap`) runs on a load was not checked") - it doesn't. **Recorded deviation from the binary:** the reference's own Notes say "the [binary's] constructors don't set them either" - only `Init`/`initHudMap` do - so this constructor's defaults are a deliberate departure from the reconstructed original, not a ported behavior; only `mapLevels`' default was actually needed to silence the warning, the other three (`mapControl`/`mapView`/`lastRevealOrigin`) are given the same treatment defensively, for the same reason (leftover memory on the savegame-restore path), at no behavioral cost on a normal spawn. `unknown1e5c` gets a constructor default too, but purely defensively - unlike those four, it IS saved/restored, so `Restore`'s own read always overwrites it on the load path anyway, the same way `Init()` does on a normal spawn. Scenario, on `e1m1`: walks (`setviewpos`, same technique as #36's own scenario) to grow `chextrek_dump`'s `hud_map` `coverage` before saving, and separately grows `mapScale` away from its default via the PDA's `map_zoom_in` command (#37's `chextrek_test_pda_map_open`/`chextrek_test_map_cmd`, then `map_stop` to freeze it - once `mapControl` is cleared, `updateMapUI`'s zoom step stops touching `mapScale` every frame regardless of what the PDA gui's own `"HudMap"` flag does afterward, so - unlike #37's own scenario - there's no ~400ms GUI-timing quirk to fight here: the value is already frozen well before the `savegame`). **Review finding fixed:** a `savegame`/`loadgame` round-trip (reused from #31's `tools/test-objectives.sh`) does **not** keep the same `idPlayer` object - `idGameLocal::InitFromSaveGame` calls `MapShutdown()` then `idRestoreGame::CreateObjects()` (`Game_local.cpp`/`gamesys/SaveGame.cpp`), which allocates a brand-new `idPlayer` via `idTypeInfo::CreateInstance` for the load, same as any other saved object - so an earlier version of this row's own claim to the contrary was wrong. What actually needed proving: `hudmap_alpha` is a process-wide global untouched by that object recreation (only `Init()`/a reveal/`showMap`/`Restore` ever write to it, none of which run between the `savegame` and the `loadgame` otherwise), so a naive "dump before save, dump after load, assert they match" check on `coverage` would have passed even with `idPlayer::Restore`'s own `hudmap_alpha` read deleted entirely. `mapScale`, by contrast, is a plain per-instance member the constructor doesn't default at all (it's saved/restored, so only `Restore`'s own `ReadFloat` ever needs to set it) - the fresh object a load creates never sets it itself, so without that read it would hold whatever the freed player's memory happened to still contain, which isn't guaranteed to differ from the pre-save value. Both weaknesses get the same fix: the scenario now perturbs both between the `savegame` and the `loadgame` - the real `showMap` console command (#38) fills `coverage` to the full `16384`, and another `map_zoom_in` grows `mapScale` further past its already-zoomed pre-save value, both asserted in their own dump - before asserting that the post-load dump's `coverage`, `scale` and `level` match the *pre-save* values, not the perturbed ones - proving the load itself, not mere inaction or memory-reuse coincidence, restored them. This is the AC's literal wording ("dump map state and coverage match the pre-save values"). The scenario separately asserts the "Location below lowest MapLevel" warning never appears, since `level` alone reads 0 on e1m1 either way (a real level 0, or `HudMapLevel`'s own fallback for a garbage `mapLevels[0]`) and so can't distinguish the two on its own. `view_x`/`view_y`/`control` (`mapView`/`mapControl`) are deliberately not asserted after the load, since they're not saved - only given a safe default (0/zero) rather than left as garbage. |
| Door opening (player + AI) | `tools/test-door-open.sh`, `tools/test-door-locked.sh`, `tools/test-ai-door-open.sh` | covered (#40+#41+#42: `idPlayer::tryOpen`, bound to `IMPULSE_16`, and the `g_doorTraceDist` cvar. #40's `tools/test-door-open.sh` drives `sf_923`'s own unlocked, `no_touch` `func_door_1`/`func_door_24` (no synthetic fixture; neither door is targeted by any other entity in `sf_923`'s own map data, so both start closed and unmoved) via `chextrek_test_impulse 16` (the same "no real bind key from a console script" gap #36's `chextrek_test_impulse` already closes): AC1 stands well within the default `g_doorTraceDist` (100) facing `func_door_1`, confirms it starts closed (`func_door_1.isOpen()` is 0 before the impulse, so the later open isn't merely inferred from `no_touch`/no-other-trigger reasoning), and asserts the use-key opens it (`func_door_1.isOpen()`, plus a new test-only `door_tryopen_count`/`door_tryopen_last` pair in `chextrek_dump`, `ChexTrekDump.cpp`, that records every time `tryOpen`'s trace resolved to an `idDoor` - the only way to tell "the trace reached the door" from "it reached nothing" independent of that door's lock state); AC2 stands well outside the default range from `func_door_24` (which also happens to have `"secret" "1"` in `sf_923`'s own map data - unrelated to this feature, just a side effect of using a real door) and asserts the use-key does nothing (`door_tryopen_count` unchanged, `func_door_24.isOpen()` stays 0), then raises `g_doorTraceDist` to 200 from the console and asserts the same use-key now opens it. **#41's `tools/test-door-locked.sh`** covers `tryOpen`'s locked-door branches, which #40 already ported as part of the whole reconstructed function but didn't exercise: AC1 (`sf_923`) uses `hbdoor1` (`locked "1"`, no `"requires"`, `"lockedtext"` "This door is locked.  It can be opened at a console on the bridge.") and asserts the tip shows with that exact text; sf_923 also has a `lockedtext` door, `brokendoor`, that was tried first and dropped - it sits immediately adjacent to a second, unlocked `unbrokendoor` at essentially the same origin (both spawned/solid by default; only a separate, unfired script thread ever hides one and shows the other), and every approach point/angle tried against that pair landed `tryOpen`'s trace on the wrong (unlocked) door instead, confirmed directly via `door_tryopen_last` rather than inferred - `hbdoor1` has no such overlapping twin. AC2 (`e1m1`) uses `func_door_13` (`"requires"` "Blue Key"): using it with no key shows the tip ("You need a Blue Key to open this door.") and the door stays shut (`isOpen()` 0), with `door_tryopen_count` rising by exactly one and `door_tryopen_last=func_door_13` proving the trace reached that specific door rather than merely inferring it from the tip alone; `spawn chex_key_blue name <n> origin "<x> <y> <z>"` + `trigger <n>` gives the key without needing mouse/impulse input or a reachable map pickup (`Cmd_Spawn_f`'s own key/value override lets the spawn land at a known-open point rather than the player's forward-facing default, which would have landed inside/behind the door's own brush); using the door again then opens it (`isOpen()` 1), with `door_tryopen_count`/`_last` checked the same way across that second use too. Three new `chextrek_dump` lines, `hud_tip_up`/`hud_tip_title`/`hud_tip_text` (`ChexTrekDump.cpp`, named `hud_tip_*` rather than `door_tip_*` since `ShowTip` is a generic stock method, not door-specific), read `idPlayer::ShowTip`'s own HUD gui state directly (`IsTipVisible()`, `hud->GetStateString( "tiptitle" | "tip" )`) rather than adding a new hook into already-ported code - `ShowTip` (called by both of `tryOpen`'s locked branches) logs nothing itself, so this is the only way a scenario can see the tip text/title from the log. **#42's `tools/test-ai-door-open.sh`** ports the last piece: `idAI::Spawn`'s `"canopendoors"` spawnArg (default `"1"`, `canOpenDoors` member) and the identical `if ( canOpenDoors ) { OpenDoors( GetSlideMoveEntity() ); }` block spliced into `AnimMove`/`FlyMove`/`SlideMove` (resolving #42's own "blocked by #40 on the assumption that player and AI share the door-open code" note: they don't - `door-opening.md`'s Notes confirm `idPlayer::tryOpen` and `idAI::OpenDoors` never call each other or share any caller, only the stock `idDoor::IsLocked`/`Use` callees - #40/#41 and #42 are independently-ported, non-overlapping code paths that both happen to open the same kind of door) - `idAI::OpenDoors`/`Event_OpenDoors`/the `openDoors` script event itself already landed with #30 (`tools/test-script-events.sh` exercises the explicit script call), but nothing before #42 called `OpenDoors` from a monster's own blocked movement. AC1 spawns a `monster_chex_biped` next to `sf_923`'s `func_door_1` and drives it into the door via the stock, already-ported `setMoveType`/`moveToPosition` script events (bypassing the AI's own script state machine, which never autonomously activates for a console-spawned monster with no sighted enemy or trigger - see the test's header for why, and why `idAI::MoveToPosition`'s own AAS pathing can't be aimed at a point past the door, since this `sf_923` AAS never connects the two sides of it regardless of the door's open state); a new `ai_opendoor_count`/`ai_opendoor_last` pair in `chextrek_dump` (`ChexTrekDump.cpp`) records every time `idAI::OpenDoors` actually activates a door, independent of `isOpen()` (mirroring `door_tryopen_count`/`_last`). Because `func_door_1` has no `"wait"`/`"toggle"` override, it auto-opens then auto-closes itself on stock `idMover_Binary` timing (~3 seconds open), so AC1 polls `isOpen()` repeatedly across that window rather than at one fixed point (confirmed necessary by an actual run: a single check landed on the wrong side of the window and read closed even though the door had already opened and closed once). AC2 repeats the identical setup with `"canopendoors" "0"` and asserts `ai_opendoor_count` never leaves 0 and every poll reads closed. Both ACs also assert `distanceToPoint()` against the `moveToPosition` target starts >50 units (right after spawn, ~76 on an actual run) and then drops by >5 units (after the walk, ~67 measured - 3D, so not purely a horizontal-progress figure) - proving the monster actually walked toward the door rather than holding still, not that `func_door_1` *specifically* is what stopped it. That second, independent proof is `ai_blocked_last`, a new test-only `chextrek_dump` line (`ChexTrekDump.cpp`'s `ChexTrek_NoteAIBlocked`) that reads `physicsObj.GetSlideMoveEntity()` unconditionally - before the `if ( canOpenDoors )` check - so it fires the same way regardless of `canopendoors`, unlike `ai_opendoor_count`/`_last` (gated by `canOpenDoors`, so useless for AC2's own criterion). `idAI::moveStatus()` was tried first as a substitute and doesn't work: an actual run showed it's driven by `SlideMove`'s AAS-level obstacle-avoidance call, a separate mechanism from the direct physics collision this scenario exercises, and it never left `MOVE_STATUS_MOVING`/`_DONE` here. Only the `SlideMove` copy of the `if ( canOpenDoors )` block is exercised directly; `decomp-so/reference/door-opening.md`'s Notes record, as an open lead, that its `FlyMove` addresses (unlike `AnimMove`/`SlideMove`) list two call sites where this port has one. |
| Trails (`mkTrail`, `idGameLocal::trails`/`BabySitTrail`/`RemoveTrail`) | `tools/test-trails.sh` | covered - `decomp-so/reference/trails.md` ported in full into a new `game/Trail.h`/`Trail.cpp` (the original mod's own added files, not stock dhewm3-sdk ones, added to `CMakeLists.txt`'s `src_game_mod` list). **Edits-inside-stock-functions leads recorded for #43** (matching the reference's own Notes): `idActor::Spawn` (`Actor.cpp`) allocates a `mkTrail` and calls its `Spawn()` when the actor's own `"hasTrail"` spawnArg is set, copying the entityDef named by `"trailDef"` into the trail's own `spawnArgs` and pointing its `owner` back at the actor; `~idActor` calls `trail->FadeTrail()` so a doomed actor's trail detaches and starts fading instead of outliving a dangling pointer; `idGameLocal::RunFrame` (`Game_local.cpp`, just before `RunDebugInfo()`) calls every live trail's `Think()`, and new `BabySitTrail`/`RemoveTrail` methods add to/remove from the new `idGameLocal::trails` list, exactly as the reference describes. **Recorded deviations from the reference (not covered by any #43 AC, called out here per spec #28's "any such change is recorded"):** `Actor.h`/`.cpp` give `hasTrail`/`trail` constructor defaults (`false`/`NULL`) the reference doesn't document, so `~idActor`'s `if ( hasTrail && trail )` (one more check than the reference's plain `if ( hasTrail )`) never reads `trail` through garbage memory on an actor whose def never sets `"hasTrail"`; `idActor::Save`/`Restore` are unedited, so a save/load never persists `hasTrail`/`trail` at all (a restored actor simply has no trail) - the reference's own Notes mark whether the binary's `idActor::Save`/`Restore` do either as unchecked, and no #43 AC exercises save/load, so this is left as an open lead rather than guessed at. **AC2 (no pointer-to-int casts):** the reference's `renderEntity.entityNum = reinterpret_cast<int>( this )` (used so the static `ModelCallback` can recover which `mkTrail` a render-world callback belongs to - `mkTrail` isn't an `idEntity`, so it has no real entity number to look up instead) is ported using `renderEntity.callbackData` instead - a `void *` field `renderer/RenderWorld.h` already documents as "used for whatever the callback wants" - so there's no cast going in and a plain `void *` to `mkTrail *` cast (never through `int`, and pointer-size-safe on any build, not just this project's 32-bit-only target) coming back out in `ModelCallback`; checked by `tools/test-trails.sh`'s own static grep; the harness's always-on checks passing separately confirms it actually built and ran, though that alone doesn't prove the absence of a cast - the grep does. **Two fixes recorded per spec #28's "fixing the original mod's own bugs...unless they crash" allowance:** (1) `mkTrail::addNewAnchor`'s no-owner branch (reached only after `FadeTrail`) indexed `anchors[ anchors.Num() - 1 ]` with no bounds check in the reference/binary - undefined behavior if a trail's owner is removed before it ever placed a single anchor; this port adds a bounds guard (return false in that case, the same outcome a failed ground trace already produces). (2) `idGameLocal::MapClear` (`Game_local.cpp`) now deletes every remaining `gameLocal.trails` entry: the reference's Notes call "where the list is emptied at map end" an unchecked open question, and since real trails' `"fadeTime" "-1"` means `FadeTrail` never deletes them (it only clears `TH_THINK`), every map's own trails were otherwise leaking into the next map load with real content (reasoned from the code, not reproduced by loading two maps back to back and counting) - and a still-fading finite-fadeTime trail caught mid-map-change (not something any real, shipped def can trigger; only the test fixture below uses a finite fadeTime, and no scenario changes maps mid-fade) would have had `RunFrame`'s `Think()` loop call into the *next* map's `gameRenderWorld` with a `modelDefHandle` that belonged to the old one - a latent crash risk this closes, not one that was reproduced live. **Scenario** (`tools/test-trails.sh`, on `sf_923`): `sf_923` places 19 `monster_flemoid` entities directly (the entityDef the "flemoid" filename doesn't literally match - `def/chex_monster_flemoid.def` declares `entityDef monster_flemoid`), one of which (`monster_flemoid_5`) overrides `"hasTrail" "0"`, so the baseline is 18 live trails, not 19 (`e1m1` also places 19 `monster_flemoid` with no such override, but no scenario runs there - #43's AC needs only one map's proof, same as several other rows in this table); `chextrek_dump`'s new `trails`/`anchors` counts are already nonzero right after map load, so every assertion is a delta against that baseline, not an absolute 0/1. Since real content's only `trailDef` (`def/slime_trail.def`'s `"trail"`) sets `"fadeTime" "-1"` (never fades, by design - `corvette_notes.txt`: "i thought the trails lasted forever"), proving the fade-to-zero half of the AC needs a finite fadeTime nothing in real content ever uses; a new test-only entityDef, `chextrek_test_trail_fastfade` (`def/slime_trail.def`, inherits `"trail"` and only shortens `fadeDelay`/`fadeTime`), closes that gap the same way `tools/test-ai-door-open.sh`'s `"canopendoors"` override does - the scenario spawns an extra `monster_flemoid` with that as its `"trailDef"` override. It walks toward a nearby point (`setMoveType`/`moveToPosition`, same stock script events #42 uses) from `sf_923`'s own `info_player_start_1` origin, proving `trails` rises by exactly 1 on spawn and holds through the walk, and proving the walk itself happened via `distanceToPoint()` before/after (same technique as `tools/test-ai-door-open.sh`, not just inferred from the trail bookkeeping); `remove`ing it proves the trail survives mid-fade (still in `gameLocal.trails`); several `wait`-and-`dump` polls past the fixture's fadeTime prove `trails` returns to the exact baseline once `Think()`'s `delete this` fires - this build's "wait" tick turned out to be ~2.5-3.5ms of simulated time each (not the ~16.7ms/tick #42's own scenario measured - "wait" ticks are real rendered frames, and frame time isn't a fixed constant), so the scenario polls several times with margin rather than gambling on one guessed wait count. **Anchor count is checked but not gating, cause unknown:** a live run showed this scenario's own spawned trail consistently lays down zero anchors of its own (at three different spawn points, including the real player-start origin used here, and confirmed still moving a real distance via the `distanceToPoint()` check above) while the map's own pre-placed flemoids' trails do have anchors already at the baseline dump. `decomp-so/reference/trails.md`'s Notes flag `mkTrail::lastPos` as never initialized (neither the constructor nor `Spawn()` sets it) as one candidate - spec #28's Out of Scope lists that exact bug as one to leave alone unless it crashes (it doesn't) - but the same Notes say that only blocks the first update if the initial distance-vs-garbage comparison itself fails (e.g. on a NaN); this wasn't confirmed to be what's actually happening here, so it's recorded as an open, undiagnosed lead rather than a confirmed cause. #43's actual acceptance criterion is the trail *count*, not the anchor count, so this is logged as `INFO`, not asserted. |
| Env shots | `tools/test-env-shots.sh` | covered - `decomp-so/reference/env-shots.md`'s `matt_func_envshot` ported in full into a new `game/func_envshot.h`/`func_envshot.cpp` (the original mod's own added files, not stock dhewm3-sdk ones, same pattern as #43's `game/Trail.h`/`.cpp`), plus the `"takeEnvShots"` console command registered right after the mod's `"showMap"` in `idGameLocal::InitConsoleCommands` (`gamesys/SysCmds.cpp`), matching the reference's own cited registration order. `def/func_envshot.def` already existed pre-#44 (its own `entityDef func_envshot`/`"spawnclass" "matt_func_envshot"`), so nothing needed adding there. No edit-inside-a-stock-function lead recorded for this sub-issue - env-shots.md's Notes record none, and `takeEnvShots` is a self-contained new command, not a splice into stock code. **Scenario** (`tools/test-env-shots.sh`, on `e1m1`, since neither shipped map places a `func_envshot` - env-shots.md's own Notes): spawns one at a known-good playable point (the same origin `tools/test-ai-door-open.sh`'s monster uses) with an explicit `name` (doubles as the entity's own name and, since `Event_envShot` reads spawnArgs' own `"name"` key, the shots' file basename), a small `size` (32, for speed) and an explicit `blends` (1, matching `R_EnvShot_f`'s own default so a missing key can't shift it), then runs `takeEnvShots`. Asserts, straight from the log: `takeEnvShots_f`'s own `"1 envShots taken"` count line (proving exactly the one fixture was found and shot via `gameLocal.entities[]`/`IsType`), the stock engine's own `"Wrote env/chextrek_test_envshot_nz.tga, etc"` confirmation line (the `envShot` console command's own write-confirmation - direct log evidence the images were written, not just inferred from `Event_envShot`'s own `BufferCommandText` call), and, independently, that all six cube faces (`_px`/`_nx`/`_py`/`_ny`/`_pz`/`_nz.tga`) exist and are nonempty in the mod's scratch save dir (`Documents/My Games/dhewm3/chextrek/env/` - fs_savepath's actual write target on this engine build, confirmed live; not this repo's own tracked `env/`, whose files use an unrelated `_back`/`_forward`/... naming convention), plus that the written face is actually 32px wide (read straight from its TGA header), proving the fixture's own `size` spawnArg - not just some size - reached the `envShot` command text. **The command text itself isn't separately asserted:** `CMD_EXEC_NOW` doesn't echo the buffered `"envShot <name> <size> <blends>"` line to the log (confirmed live - only `takeEnvShots_f`'s own count line and the engine's `Wrote` confirmation appear), so "the log shows its envShot commands" is proven by that count line plus the `Wrote`/width evidence that the command actually ran with this fixture's own arguments, not by grepping the literal command text. Verified green across three back-to-back runs. **Open questions from the reference, still open:** env-shots.md's own "not checked" question of whether `RenderScene` sets `tr.primaryView` when called from a console command outside the frame is *not* actually settled by this scenario - a stale `tr.primaryView` left over from the last rendered frame (the player's own, non-null in a running game) would also write six correctly-sized faces, just from that view instead of the entity's; none of this scenario's assertions depend on which view was actually copied, so this stays an open lead, not confirmed. Also left open, per that same list: whether `atSpawn`'s 250ms-later shot behaves identically to `takeEnvShots`'s direct call (this scenario only exercises the console-command path, matching the AC's literal wording) and the per-shot `renderView_t` leak (136 bytes each; left as-is - spec #28's Out of Scope is "fixing the original mod's own bugs", with one exception, "unless they crash" - this leak doesn't, and this sub-issue's own AC doesn't touch it either). **Not archived as a scenario artifact:** the six written faces sit under the scratch save dir's `env/` subfolder, which `tools/lib-harness.sh`'s archiving loop doesn't copy out (it only archives top-level files and `screenshots/`) - unlike the screenshot this scenario also takes, they aren't preserved past the next harness run; this scenario reads them directly from the scratch dir (`CHEXTREK_MOD_SAVE_DIR`, now printed by `tools/run-scenario.sh`) right after its own run, before that happens. |
| Worldspawn music volume | `tools/test-music-volume.sh` | covered - `decomp-so/reference/worldspawn.md`'s `idWorldspawn::Save`/`Think` ported into the existing stock `game/WorldSpawn.h`/`.cpp` (a stock file, unlike #43/#44's own new files), plus `g_MusicVolume` (a menu-slider cvar, `guis/mainmenu.gui`'s "Ingame Music" slider) and a second, `static`-linkage `EV_FadeSound` local to `WorldSpawn.cpp` (see below). **Edit-inside-stock-`Spawn` lead recorded for #45** (matching the reference's own Notes): `idWorldspawn::Spawn` now calls `g_MusicVolume.SetModified()` and `BecomeActive( TH_THINK )`, so `Think` applies the cvar once at every map start (not just when the cvar changes mid-map) and again each time the menu's slider changes it afterward. **`Save` overload, recorded deviation:** the reference's own `void Save( idSaveGame *savefile );` doesn't compile once both `idWorldspawn::Save` overloads (the file's own pre-existing, stock `Save( idRestoreGame * )` typo, left untouched, and this one) are in scope - `CLASS_DECLARATION`'s `(void (idClass::*)(idSaveGame*) const)&Save` cast needs an exact function-type match to pick the right one, so this port declares it `const` (matching `idEntity::Save`'s own qualifier, `Entity.h`) instead; the body is empty either way, so this is a compile-only fix, not a behavior change. **`EV_FadeSound`, recorded deviation:** the reference's own header/Implementation split (an `extern` declaration plus a plain, non-`extern` definition in a different file) doesn't link on this build - this MSVC build gives a plain top-level `const idEventDef` external linkage regardless of `extern`, so `Entity.cpp`'s own same-named `EV_FadeSound` and this port's clashed (`LNK2005`); made `static` instead (confined to `WorldSpawn.cpp`), which works identically for the constructor's name-based event-number lookup (`idEventDef`'s constructor finds the name already registered by `Entity.cpp`'s own `EV_FadeSound` and reuses its event number either way) and needs no cross-file linkage at all. **Scenario** (`tools/test-music-volume.sh`, on `e1m1` - its worldspawn sets `s_shader`/`s_volume`/`s_looping`/`s_global`, so `Think` actually reaches its start/fade branch): one `map e1m1`, then three live `set g_MusicVolume <N>` console commands in that same session - 80 (still audible), 0 (the "stop the music" branch), 45 (audible again, resuming) - each followed by a `wait` and a `chextrek_dump`, directly exercising the AC's own path (the slider changing the cvar while already in a map). **Timing margin, found while writing this scenario:** a `wait 5` between each `set` and its `chextrek_dump` was not reliably enough for `Think` to see the change before the dump ran (not a cvar-range or engine issue - per #43/#44's own findings, `wait` ticks are real rendered frames, a few ms each on this build, not a fixed server-tick count, so a handful can land short of even one real game/server frame); `wait 30` reproduced cleanly across repeated runs and is what the scenario uses. Three new `chextrek_dump` lines (`ChexTrekDump.cpp`'s `ChexTrek_NoteMusicVolume`, a test-only hook `Think` calls right after each apply, since `Think` and the stock `StopSound`/`StartSoundShader`/`Event_FadeSound` calls it makes log nothing an AFK harness can read): `music_volume_applied_count` (proves `Think` actually re-ran - asserted strictly increasing at each of the four dumps, not just present), `music_volume_last` (the cvar's own value at that apply), `music_volume_stopped` (whether that apply stopped the music). Each dump is asserted against its own expected value/stopped-state, proving the AC's literal wording ("the music volume follows it") live, in one continuous session, including the stop-the-music branch and a subsequent resume. |
| New Game plays `sf_923` clean and its exit loads `e1m1`, which plays clean (spec #28's own Done criteria, split out as #46) | `tools/test-new-game.sh` | covered - not a new port (every blocker #46's own issue text names - `trigger_objective`/`target_endlevelgui` on `sf_923`, `target_endlevelgui` on both maps, locked/`requires` doors on both maps, flemoids with `trailDef` on both maps - is already ported and separately scenario-tested by #31/#33/#34/#41/#43 above), but the first scenario to run both real maps back to back, in the real order a player reaches them, in one session - proving nothing about running them together regresses, which no single-map scenario above can. **"New Game from the main menu" - `startgame sf_923` confirmed unreachable from a console script:** `guis/mainmenu.gui`'s own New Game button flow (`windowDef AnimNewGame`'s `onTime 2400 { set "cmd" "startgame sf_923" ; }`) issues `startgame sf_923` as a GUI `"cmd"` pseudo-command, not a registered `idCmdSystem` console command - typing it into a console script reproducibly logs `Unknown command 'startgame'` (confirmed live), the same class of mouse-only gap `#34`'s stats-screen buttons and `#37`/`#38`'s PDA-map buttons already hit. `startgame <map>` is documented (id Tech 4's own general session behavior, not confirmed against this build's own source, which isn't in this repo) to itself just run `disconnect` then `map <map>` - the same `gameLocal.sessionCommand = "map " + name` transition every other scenario in this suite already drives directly - so the scenario boots to the main menu first (confirmed live: `chextrek_dump`'s first call, before any `map` command, shows `level_stats`/`hud_map` both `none`, proving no level is loaded yet) and then uses plain `map sf_923`, same as every other scenario. **"its exit (`nextMap` `e1m1`) loads `e1m1`" - drives the real exit entity directly, not the whole GUI chain:** the real, shipped path from `sf_923`'s stats screen to `e1m1` is a multi-step GUI chain (trigger `target_endlevelgui_1`'s `stats.gui` -> its state 5 auto-`ActivateTargets`'s its own `"target"`, `target_endlevelgui_2`'s `end_trek.gui` -> a mouse click on `end_trek.gui`'s `e1m1_button` -> `runScript map_storage_facility::end_trek_e1m1` -> `sys.trigger( $target_endlevel_3 )`) - already fully described, and already out of scope, by `#34`'s own `tools/test-end-level-nextmap.sh` header comment (neither of `sf_923`'s own `target_endlevelgui` entities carries a `"nextmap"` spawnArg; the real `"nextMap"` `"e1m1"` key lives on `target_endlevel_3`, a plain stock, unmodified `idTarget_EndLevel` entity, `maps/sf_923.map:35103-35106`). That whole GUI chain's own mechanics are `#33`/`#34`'s job, already covered, not `#46`'s - a live attempt here to drive it end-to-end via a bare `script map_storage_facility::end_trek_e1m1()` console call (mimicking the click) registered `target_endlevelgui_2`'s own GUI (confirmed live via `chextrek_dump`'s `customui: active`) but never actually retriggered `target_endlevel_3` within a generous wait, for a reason not tracked down (the function's own `sys.wait( 1 )` needs a running script thread, which a directly-called, non-`thread`-prefixed console `script` expression may not provide the same way a real `runScript` GUI command's own `idThread` wrapping does - unconfirmed, not worth chasing further since it isn't this sub-issue's own feature to prove). `#46`'s actual AC is narrower and more literal than "reproduce the whole GUI chain": the real, already-placed exit entity's own `nextMap` wiring works. `trigger target_endlevel_3` fires that real, unmodified stock entity directly (the same `Cmd_Trigger_f` technique every other scenario in this suite already uses to fire a specific real entity, e.g. `#33`'s `trigger func_door_16`), confirmed live to load `e1m1` clean. **Scenario** (`tools/test-new-game.sh`): boot to the main menu (dump #1), `map sf_923`, three `wait 100`/`chextrek_dump` pairs (dumps #2-4, 300 frames total - "plays clean for N frames" as *sustained*, not just instantaneous, cleanliness), `trigger target_endlevel_3`, then the same three-dump, 300-frame pattern on `e1m1` (dumps #5-7). The always-on checks already scan the *whole* log for `ERROR`/unknown-event/unknown-spawnclass/script-compile lines regardless of which map is current, so on top of those this scenario's own assertions are keyed off `chextrek_dump`'s field lines *by dump position* (`tools/lib-harness.sh`'s `chextrek_line_field_values`, e.g. the 2nd/3rd/4th `trails:` value read across the whole log is unambiguously sf_923's own 3 post-load dumps, since the console script emits exactly 7 `chextrek_dump` calls in a fixed order) rather than by log line-number ranges - avoiding a log-buffering quirk found while writing this scenario (a stray, bodyless `CHEXTREK-STATE-DUMP v1` header occasionally gets flushed ahead of its own dump's real field lines during the heavy script-reload spam a `map` command causes; every dump's own fields still show up, in order, exactly once each, just sometimes with an extra bare header line nearby). Asserts, straight from the log: dump #1 shows `level_stats`/`hud_map` both `none` (no level loaded before `map sf_923`); `<N> msec to load sf_923` then (strictly later in the log) `<N> msec to load e1m1`, proving the real order a player reaches them; exactly 7 `trails:` lines total (1 main menu + 3 `sf_923` + 3 `e1m1`); dumps #2-4 all read `trails=18` and the same `level_stats` totals (`.../27 .../30 .../1`) - `sf_923`'s own known-stable baseline (#43's own coverage row); and dumps #5-7 all read `trails=19` and `.../22 .../34 .../3` - `e1m1`'s own 19 placed `monster_flemoid`s, none overriding `"hasTrail"`, proving `#46`'s own "flemoids with `trailDef` (both)" blocker is alive and error-free on `e1m1`'s side too, not just `sf_923`'s. Verified green across three back-to-back full runs. |
