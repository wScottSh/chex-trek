## Agent skills

### Issue tracker

Issues tracked in GitHub Issues (via `gh`). See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical triage labels, default names. See `docs/agents/triage-labels.md`.

### Wayfinder

Map #1 charts the "runs on modern Windows" effort. Note: the original custom game-code source **does not exist** — confirmed by the authors (the repo owner is one). Do not propose recovering or requesting it.

### Domain docs

Single-context (`CONTEXT.md` + `docs/adr/` at root). See `docs/agents/domain.md`.
